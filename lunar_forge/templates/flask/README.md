# Flask starter

This starter provides a small JSON endpoint and a test using Flask's built-in
test client.

## Install

```bash
python -m pip install -r requirements.txt
```

## Run

```bash
flask --app app run --debug
```

Visit <http://127.0.0.1:5000>.

## Test

```bash
python -m unittest -q
```
