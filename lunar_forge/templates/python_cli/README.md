# Python CLI starter

This starter uses `argparse` and `unittest` from the Python standard library.
It has no third-party dependencies.

## Run

```bash
python app.py --name Ada
```

## Test

```bash
python -m unittest -q
```
