# Static website starter

This project uses plain HTML and CSS with no external dependencies.

## Run locally

Open `index.html` directly, or start Python's built-in web server:

```bash
python -m http.server 8000
```

Then visit <http://localhost:8000>.
