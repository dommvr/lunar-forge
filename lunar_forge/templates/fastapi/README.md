# FastAPI starter

This starter provides a small JSON endpoint and an import-level unit test.

## Install

```bash
python -m pip install -r requirements.txt
```

## Run

```bash
uvicorn app:app --reload
```

Visit <http://127.0.0.1:8000/docs> for the interactive API documentation.

## Test

```bash
python -m unittest -q
```
